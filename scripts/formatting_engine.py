from formatting import format_bullets, format_spacing

# ...existing code...

def apply_formatting(text):
    """Apply formatting to text."""
    formatted_text = format_bullets(text)
    formatted_text = format_spacing(formatted_text)
    return formatted_text

# ...existing code...
